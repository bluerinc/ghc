{
  "name": "gh-api-interaction",
  "version": "1.0.0",
  "description": "Express endpoint designed for invocation via a webhook that is triggered upon the creation of a new repository within a pre-configured GitHub organization.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kevin Bluer",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.2",
    "body-parser": "^1.19.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1"
  }
}
